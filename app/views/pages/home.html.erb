<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Home</title>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>
<body>
  <h1>Pages#home</h1>
  <p>Find me in app/views/pages/home.html.erb</p>

  <div class="container">
    <%= link_to 'Login', new_user_session_path, class: 'btn btn-primary btn-lg btn-block' %>
    <%= link_to 'Registrase', new_user_registration_path, class: 'btn btn-secondary btn-lg btn-block' %>
    <%= link_to 'Ver mis notas', new_user_registration_path, class: 'btn btn-secondary btn-lg btn-block' %>
  </div>

  <div class="container">
    <% if user_signed_in? %>
      <p><%= current_user.email %></p>
    <% else %>
      <p>Ningun usuario logeado</p>
    <% end %>

  </div>

</body>
</html>
